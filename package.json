{
  "name": "macau-sync-server",
  "version": "1.0.0",
  "description": "澳门洗码报表同步服务器",
  "main": "server.js",
  "scripts": {
    "start": "node build.js && node server.js",
    "build": "node build.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
