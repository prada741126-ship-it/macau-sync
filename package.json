{
  "name": "macau-sync-server",
  "version": "11.2.11",
  "description": "澳门洗码报表同步服务器",
  "main": "server.js",
  "scripts": {
    "start": "rm -rf dist/ && node build.js && node server.js",
    "build": "node build.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
