# 澳門洗碼報表 — 正式版 1.8 完整文檔

> **版本**：v11.1.8  
> **日期**：2026-06-09  
> **部署網址**：https://macau-sync-production-4a7b.up.railway.app  
> **密碼**：`macau888`  
> **黃金基準**：`index_v10.26_GOLDEN.html`（永不修改）  
> **工作目錄**：`C:\Users\monkey888\WorkBuddy\Claw\render-deploy\`

---

## 目錄

1. [格式結構](#1-格式結構)
2. [代碼架構](#2-代碼架構)
3. [函數清單](#3-函數清單)
4. [用語表](#4-用語表)
5. [CSS 設計令牌](#5-css-設計令牌)
6. [資料架構](#6-資料架構)
7. [Firebase 架構](#7-firebase-架構)
8. [鍵盤快捷鍵](#8-鍵盤快捷鍵)
9. [版本沿革](#9-版本沿革)

---

## 1. 格式結構

### 1.1 HTML 結構

```
<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <!-- 設計令牌 (Design Tokens) -->
  <!-- 全域 CSS -->
    |- base.css       (設計令牌 + 全域重置)
    |- layout.css     (sidebar/main-content/topbar)
    |- components.css (按鈕/卡片/表格/Toast/表單)
    |- china-theme.css(中國風主題)
    |- mobile.css     (手機版響應式)
  -->
</head>
<body>
  <!-- 登入覆罩 (Password Overlay) -->
  <div id="pw-overlay"> ... </div>
  <canvas id="pw-particles"> (登入頁面粒子動畫)

  <!-- 主應用外框 -->
  <div id="app-shell">
    <!-- 側邊欄 (Sidebar) -->
    <div id="sidebar">
      <div class="sb-logo"> ... </div>
      <div class="sb-month"> ... </div>
      <div class="sb-nav"> ... </div>
      <div class="sb-toggle"> ... </div>
      <div class="sb-version"> ... </div>
    </div>

    <!-- 主內容區 -->
    <div id="main-content">
      <div id="topbar"> ... </div>
      <div id="page-scroll">
        <!-- 五個頁面 -->
        <div id="page-overview" class="page"> ... </div>
        <div id="page-all" class="page"> ... </div>
        <div id="page-query" class="page"> ... </div>
        <div id="page-summary" class="page"> ... </div>
        <div id="page-room" class="page"> ... </div>
      </div>
    </div>
  </div>

  <!-- 彈窗 (Modals) -->
  <div id="modal"> ... </div>              (新增/編輯交易)
  <div id="fund-modal"> ... </div>         (公基金)
  <div id="agent-wallet-modal"> ... </div>  (代理錢包)
  <div id="agent-mgr-modal"> ... </div>    (代理管理)
  <div id="hc-modal-bg"> ... </div>       (酒店配置)
  <div id="shortcut-help-modal"> ... </div> (快捷鍵說明)

  <!-- 載入覆罩 -->
  <div id="loading-overlay"> ... </div>

  <!-- Toast 容器 -->
  <div id="toast-container"> ... </div>

  <!-- Firebase + CryptoJS + Chart.js -->
  <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js"></script>
  <script src="https://www.gstatic.com/firebasejs/8.10.1/firebase-database.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/4.1.1/crypto-js.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>

  <!-- 應用程式碼 -->
  <script>
    // === 全域變數 ===
    // === 工具函數 ===
    // === 認證/導航 ===
    // === 交易 CRUD ===
    // === 同步系統 ===
    // === 房務系統 (RM) ===
    // === 酒店配置 (HC) ===
  </script>
</body>
</html>
```

### 1.2 CSS 架構

| 層級 | 變數前綴 | 說明 |
|------|-----------|------|
| 背景層 | `--bg-*` | 60-70% 深色背景 |
| 邊框層 | `--border-*` | 細描邊 · 低透明度 |
| 文字層 | `--text-*` | 15-20% 中性色 |
| 科技點綴 | `--tech-*`, `--sky-*`, `--electric-*` | 10-15% 冷色 |
| 中式點綴 | `--gold-*`, `--vermilion*` | 5-10% 金/紅 |
| 狀態色 | `--success`, `--warning`, `--danger`, `--status-*` | Fintech 風格 |

### 1.3 頁面結構

| 頁面 ID | 名稱 | 主要功能 |
|---------|------|---------|
| `#page-overview` | 總覽 | KPI 卡片、趨勢圖、代理排名、場地分佈、最近活動 |
| `#page-all` | 全部交易 | 交易明細表格、新增/編輯/刪除、CSV 匯入/匯出 |
| `#page-query` | 查詢 | 進階篩選、場地篩選、快速篩選 |
| `#page-summary` | 統計 | 月度統計、代理統計、圖表 |
| `#page-room` | 房務系統 | 訂房管理、日租/月租、酒店配置 |

---

## 2. 代碼架構

### 2.1 檔案組織

```
render-deploy/
├── build.js                 # 零依賴建構工具
├── template.html            # HTML 骨架（含佔位符）
├── version.json             # 版本號
├── index.html              # 開發版本（模組化來源）
├── dist/
│   └── index.html         # 建構輸出（部署用）
├── css/
│   ├── base.css          # 設計令牌 + 全域重置
│   ├── layout.css        # sidebar/main-content/topbar
│   ├── components.css    # 按鈕/卡片/表格/Toast/表單
│   ├── china-theme.css   # 中國風主題（隱性克制）
│   └── mobile.css       # 手機版響應式
└── js/
    ├── config.js         # PRODUCTION、UI_COLORS、同步配置
    ├── storage.js        # STORAGE_KEYS
    ├── utils.js          # 工具函數
    ├── crypto.js         # simpleCrypt()
    ├── ui/toast.js       # showToast()
    ├── data/
    │   ├── transactions.js
    │   ├── fund.js
    │   ├── agent-wallets.js
    │   ├── agent-list.js
    │   └── backup.js
    ├── auth.js
    ├── auth-navigation.js
    ├── navigation.js
    ├── sync.js
    ├── pages/
    │   ├── query.js
    │   ├── overview.js
    │   ├── overview-charts.js
    │   ├── rank-chart.js
    │   ├── room.js
    │   └── room-misc.js
    ├── hotel-config-presets.js
    ├── hotel-config-logic.js
    └── mobile.js
```

> **工作流程**：改 `css/*.css` 或 `js/**/*.js` → `node build.js` → `git push` → Railway 自動部署

### 2.2 全域變數

```javascript
// 資料儲存
var txs = [];                    // 交易資料陣列
var fundWithdrawals = [];        // 公基金提領紀錄
var agentWallets = {};           // 代理錢包 { agentName: [records] }
var agentList = [];              // 代理清單

// ID 計數器
var editId = null;               // 編輯中的 _fbKey
var nextId = 1;                 // 下一個交易 ID
var fundNextId = 1;             // 下一個公基金 ID
var agentWalletNextId = 1;      // 下一個代理錢包 ID

// Firebase 相關
var _migrated = false;           // 是否已完成 Firebase 物件格式遷移
var _syncConnected = true;       // Firebase 連線狀態監控
var SYNC_PASSWORD = '';          // 同步密碼（使用者輸入後設定）

// 狀態變數
var workingMonth = "";           // 當前工作月份，如 "2026-06"
var sortState = { table: "", col: "", asc: true };  // 排序狀態
var _draftTimer = null;         // 草稿自動儲存計時器
```

### 2.3 儲存金鑰常數

```javascript
var STORAGE_KEYS = {
  DATA:          "macau_data",
  FUND:          "macau_fund_data",
  AGENT_WALLETS: "macau_agent_wallets",
  AGENT_LIST:     "macau_agent_list",
  DRAFT:         "macau_draft",
  CONFIG:         "macau_config",
  ARCHIVES:       "macau_archives",
  SAVED_FILTERS: "macau_saved_filters",
  BACKUP_LIST:    "macau_backup_list",
  BACKUP_PREFIX:  "macau_backup_",
  WORKING_MONTH:  "macau_working_month",
  AUTH:          "macau_auth",
  LAST_BACKUP_DATE: "macau_last_backup_date",
  RM_BOOKINGS:   "rm_bookings",
  RM_LAST_ID:    "rm_last_id",
  HC_CONFIG:      "hc_config",
};
```

---

## 3. 函數清單

### 3.1 認證與導航

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `isRemoteAccess()` | 4840 | 檢查是否從外部網路存取 |
| `showPasswordOverlay()` | 4845 | 顯示密碼輸入覆罩 |
| `togglePw()` | 4856 | 切換密碼輸入框顯示/隱藏 |
| `resetSession()` | 4863 | 重設會話計時器 |
| `checkPw()` | 4872 | 檢查密碼是否正確 |
| `showPage(name, sidebarEl)` | 4920 | 切換顯示指定頁面 |
| `toggleSidebar()` | 4969 | 切換側邊欄收合/展開 |
| `toggleMobileSidebar()` | 4976 | 切換手機版側邊欄 |
| `isModalOpen()` | 4774 | 檢查是否有彈窗開啟 |

### 3.2 工具函數

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `_fbKey()` | 4317 | 產生 Firebase push() 金鑰 |
| `_syncSet(ref, data)` | 4322 | 同步寫入重試封裝（指數退避） |
| `getDow(ds)` | 4382 | 取得日期的星期幾 |
| `nowStr()` | 4390 | 取得目前日期字串 (YYYY-MM-DD) |
| `toNum(s)` | 4399 | 將字串轉為數字（支援萬、逗號） |
| `fmt(n)` | 4408 | 數字千分位格式化 |
| `calcComm(vol, rate)` | 4420 | 計算佣金 (volume × rate / 10000) |
| `calcFund(comm, bonus)` | 4424 | 計算公基金 (comm - bonus) |
| `getSessionPw()` | 4434 | 從 sessionStorage 讀取密碼 |
| `setSessionPw(pw)` | 4438 | 設定 session 密碼 |
| `clearSessionPw()` | 4442 | 清除 session 密碼 |
| `encryptData(obj)` | 4446 | AES 加密資料 |
| `decryptData(str, silent)` | 4455 | AES 解密資料 |
| `encryptWallets(obj)` | 4472 | 加密代理錢包資料 |
| `decryptWallets(str)` | 4481 | 解密代理錢包資料 |
| `lockBody()` | 4499 | 鎖定頁面捲動 |
| `unlockBody()` | 4500 | 解除頁面捲動鎖定 |
| `escHtml(s)` | 8035 | HTML 跳脫（防止 XSS） |

### 3.3 通知系統

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `showToast(msg, type)` | 4503 | 顯示 Toast 通知 |
| `showToastAction(msg, type, actionLabel, actionFn)` | 4517 | 顯示帶操作按鈕的 Toast |
| `showErrorRecovery(elOrId, title, desc, retryFn)` | 4532 | 顯示錯誤恢復提示 |

### 3.4 交易管理

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `sortTable(tableId, thEl)` | 4544 | 表格排序 |
| `sortTxs(arr)` | 4564 | 交易資料排序 |
| `saveDraft()` | 4585 | 儲存表單草稿 |
| `loadDraft()` | 4610 | 讀取表單草稿 |
| `clearDraft()` | 4621 | 清除表單草稿 |
| `_setupDraftAutoSave()` | 4628 | 設定草稿自動儲存監聽 |
| `openModal(id)` | 5018 | 開啟新增/編輯交易彈窗 |
| `_resetFormFields()` | 5087 | 重設表單欄位 |
| `closeModal()` | 5105 | 關閉交易彈窗 |
| `toggleTypeFields()` | 4996 | 切換洗碼/現金寄放欄位顯示 |
| `validateMonthDate(dateStr, label)` | 5007 | 驗證日期是否為當月 |
| `calc()` | 5111 | 計算佣金/公基金 |
| `saveForm()` | 5123 | 儲存交易表單 |
| `deleteTx(fbKey)` | 4782 | 刪除交易（含確認對話框） |
| `closeCurrentMonth()` | 4724 | 月末結算 |

### 3.5 資料儲存

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `saveData(silent)` | 5642 | 儲存交易資料到 localStorage + 同步 |
| `loadData(silent)` | 5671 | 從 localStorage 讀取交易資料 |
| `renderAll()` | 5697 | 渲染全部交易表格 |
| `_objToArray(obj)` | 5822 | 物件轉陣列（Firebase 格式相容） |
| `_arrToObj(arr)` | 5835 | 陣列轉物件 |
| `_awArrToObj(aw)` | 5846 | 代理錢包陣列轉物件 |

### 3.6 公基金

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `showLoading(msg)` | 5361 | 顯示載入覆罩 |
| `hideLoading()` | 5368 | 隱藏載入覆罩 |
| `openFundModal()` | 5373 | 開啟公基金彈窗 |
| `closeFundModal()` | 5390 | 關閉公基金彈窗 |
| `saveFundForm()` | 5395 | 儲存公基金表單 |
| `deleteFund(id)` | 5418 | 刪除公基金紀錄 |
| `saveFundData(silent)` | 5443 | 儲存公基金資料 |
| `loadFundData(silent)` | 5455 | 讀取公基金資料 |

### 3.7 代理錢包

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `openAgentWalletModal(agentName)` | 5473 | 開啟代理錢包彈窗 |
| `closeAgentWalletModal()` | 5497 | 關閉代理錢包彈窗 |
| `saveAgentWalletForm()` | 5502 | 儲存代理錢包表單 |
| `deleteAgentWallet(agent, id)` | 5530 | 刪除代理錢包紀錄 |
| `syncAgentDrawn(agent)` | 5591 | 同步代理已領金額 |
| `saveAgentWallets(silent)` | 5611 | 儲存代理錢包資料 |
| `saveAgentWalletsLocalOnly()` | 5622 | 僅本地儲存代理錢包 |
| `loadAgentWallets(silent)` | 5626 | 讀取代理錢包資料 |

### 3.8 代理管理

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `loadAgentList()` | 6908 | 讀取代理清單 |
| `saveAgentList()` | 6913 | 儲存代理清單 |
| `populateAgentDropdown()` | 6925 | 填充代理下拉選單 |
| `openAgentMgr()` | 6936 | 開啟代理管理彈窗 |
| `closeAgentMgr()` | 6943 | 關閉代理管理彈窗 |
| `renderAgentMgr()` | 6951 | 渲染代理管理列表 |
| `addAgentFromMgr()` | 6964 | 從管理彈窗新增代理 |

### 3.9 同步系統

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `syncUpload(onComplete)` | 5862 | 上傳資料到 Firebase |
| `syncDownload()` | 5988 | 從 Firebase 下載資料 |
| `manualSync()` | 5993 | 手動觸發同步 |
| `initSync()` | 6007 | 初始化 Firebase 同步監聽 |

### 3.10 備份系統

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `_autoBackupCheck()` | 5203 | 自動備份檢查（7 天保留） |
| `doBackup()` | 5212 | 執行備份 |
| `_cleanOldBackups()` | 5231 | 清理舊備份（保留 7 天） |
| `getBackupList()` | 5246 | 取得備份清單 |
| `restoreFromBackup(dateStr)` | 5250 | 從備份還原資料 |
| `showBackupModal()` | 5268 | 顯示備份管理彈窗 |
| `exportBackupJSON()` | 5327 | 匯出備份為 JSON 檔案 |
| `closeBackupModal()` | 5349 | 關閉備份管理彈窗 |

### 3.11 統計與圖表

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `renderSummary()` | 6229 | 渲染統計頁面 |
| `renderOverview()` | 7045 | 渲染總覽頁面 |
| `renderTrendChart(daily, monthLabel)` | 7218 | 渲染趨勢圖 |
| `renderRankChart(agtArr)` | 7311 | 渲染代理排名圖 |

### 3.12 查詢系統

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `quickFilter(type, ev)` | 6279 | 快速篩選 |
| `fillAgent()` | 6305 | 填充代理選單 |
| `toggleAdvFilter()` | 6329 | 切換進階篩選 |
| `saveCurrentFilter()` | 6341 | 儲存目前篩選條件 |
| `showSavedFilters()` | 6360 | 顯示已儲存篩選 |
| `loadSavedFilter(name)` | 6388 | 載入已儲存篩選 |
| `deleteSavedFilter(name)` | 6403 | 刪除已儲存篩選 |
| `closeFilterModal()` | 6413 | 關閉篩選彈窗 |
| `populateVenueDropdown()` | 6419 | 填充場地下拉選單 |
| `doQuery()` | 6428 | 執行查詢 |
| `renderFilterTags()` | 6832 | 渲染篩選標籤 |
| `filterByVenue(venue)` | 6849 | 按場地篩選 |
| `renderAllMiniKPI()` | 6858 | 渲染迷你 KPI |
| `getStatusTags(t)` | 6882 | 取得狀態標籤 |

### 3.13 CSV 匯入/匯出

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `csvEscape(val)` | 6686 | CSV 欄位跳脫（RFC 4180） |
| `csvParseRow(line)` | 6694 | 解析 CSV 列（支援引號、換行） |
| `exportCSV()` | 6726 | 匯出交易為 CSV |
| `importCSV(e)` | 6740 | 匯入 CSV 檔案 |

### 3.14 房務系統 (RM)

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `rmOpenModal(id)` | 7999 | 開啟訂房彈窗 |
| `rmCloseModal()` | 8000 | 關閉訂房彈窗 |
| `rmUpdatePrice()` | 8001 | 更新房價 |
| `rmOnCasinoChange()` | 8002 | 赌场選擇變更處理 |
| `rmOnHotelChange()` | 8003 | 酒店選擇變更處理 |
| `rmOnRoomChange()` | 8004 | 房型選擇變更處理 |
| `rmCalcNights()` | 8005 | 計算住宿夜數 |
| `rmOnDateChange()` | 8006 | 日期變更處理 |
| `rmCalcTotal()` | 8007 | 計算總金額 |
| `rmSaveForm()` | 8008 | 儲存訂房表單 |
| `rmRender()` | 8009 | 渲染訂房列表 |
| `rmExportCSV()` | 8010 | 匯出訂房 CSV |
| `rmImportCSV()` | 8011 | 匯入訂房 CSV |
| `rmHandleImport(e)` | 8012 | 處理訂房匯入 |
| `switchRoomTab(name, btn)` | 8022 | 切換房務頁籤 |

### 3.15 酒店配置 (HC)

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `initHC()` | 8134 | 初始化酒店配置 |
| `loadHCConfig()` | 8147 | 讀取酒店配置 |
| `saveHCConfig()` | 8167 | 儲存酒店配置 |
| `hcGetCasinos()` | 8173 | 取得赌场清單 |
| `hcGetHotels(casino)` | 8184 | 取得酒店清單 |
| `hcPopulateFilters()` | 8192 | 填充篩選器 |
| `hcFilter()` | 8205 | 篩選酒店配置 |
| `hcRenderTable(filterCasino, filterHotel)` | 8218 | 渲染酒店配置表格 |
| `hcOpenModal(idx)` | 8267 | 開啟酒店配置彈窗 |
| `hcOnCasinoChange()` | 8301 | 赌场選擇變更處理 |
| `hcCloseModal()` | 8310 | 關閉酒店配置彈窗 |
| `hcSaveModal()` | 8315 | 儲存酒店配置彈窗 |
| `hcDelete(idx)` | 8338 | 刪除酒店配置 |
| `hcReset()` | 8349 | 重設酒店配置 |
| `hcAddCasino()` | 8378 | 新增赌场 |
| `hcAddHotel()` | 8390 | 新增酒店 |
| `hcConfirmQuick()` | 8413 | 快速配置確認 |

### 3.16 動畫與效果

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `_startLoginParticles()` | 4646 | 啟動登入頁面粒子動畫 |
| `_stopLoginParticles()` | 4706 | 停止登入頁面粒子動畫 |
| `animateKPIValue(el, targetValue)` | 8520 | KPI 數字動畫 |
| `openShortcutHelp()` | 8540 | 開啟快捷鍵說明 |
| `closeShortcutHelp()` | 8544 | 關閉快捷鍵說明 |

### 3.17 工具函數（高階）

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `debounce(func, wait)` | 8591 | 防抖函數 |
| `throttle(func, limit)` | 8601 | 節流函數 |
| `setMonth(offset, page, el)` | 8696 | 設定月份篩選 |
| `setYear(page, el)` | 8716 | 設定年份篩選 |
| `setAll(page, el)` | 8733 | 設定顯示全部 |
| `filterTxsByTime(txs)` | 8749 | 按時間篩選交易 |

### 3.18 總錢包

| 函數名稱 | 行號 | 說明 |
|-----------|------|------|
| `calcTotalWallet()` | 5765 | 計算總錢包金額 |
| `updateTotalWalletUI()` | 5809 | 更新總錢包 UI |

---

## 4. 用語表

### 4.1 業務術語

| 術語 | 英文 | 說明 |
|------|------|------|
| 洗碼 | Rolling / Wash Code | 賭場術語，指玩家下注的金額 |
| 碼量 | Volume | 洗碼金額 |
| 佣金 | Commission | 按碼量計算的佣金 (volume × rate / 10000) |
| 公基金 | Fund | 從佣金扣除的基金 (comm - bonus) |
| 現金寄放 | Cash Deposit | 代理寄放現金 |
| 已領 | Drawn | 代理已領取的金額 |
| 未領 | Undrawn | 代理尚未領取的金額 (bonus - drawn) |
| 代理 | Agent | 介紹客人的中介人 |
| 場地 | Venue | 賭場或酒店 |
| 客人 | Client | 賭客 |

### 4.2 UI 用語

| 用語 | 位置 | 說明 |
|------|------|------|
| 總覽 | 側邊欄 | 儀表板總覽頁面 |
| 全部交易 | 側邊欄 | 所有交易明細 |
| 查詢 | 側邊欄 | 進階搜尋與篩選 |
| 統計 | 側邊欄 | 月度/代理統計報表 |
| 房務系統 | 側邊欄 | 訂房管理系統 |
| 新增交易 | 按鈕 | 開啟新增交易彈窗 |
| 編輯 | 按鈕 | 開啟編輯交易彈窗 |
| 刪除 | 按鈕 | 刪除交易（含確認） |
| 公基金 | 按鈕 | 公基金管理 |
| 代理錢包 | 連結 | 查看代理錢包明細 |
| 代理管理 | 按鈕 | 管理代理清單 |
| 備份管理 | 按鈕 | 備份與還原 |
| 同步 | 按鈕 | 手動觸發 Firebase 同步 |
| 匯出 CSV | 按鈕 | 匯出交易為 CSV |
| 匯入 CSV | 按鈕 | 從 CSV 匯入交易 |

### 4.3 技術術語

| 術語 | 說明 |
|------|------|
| `_fbKey` | Firebase push() 產生的唯一金鑰 |
| `editId` | 編輯中的交易 _fbKey |
| `workingMonth` | 當前工作月份，用於月末結算 |
| `silent` | 參數，設為 true 時不顯示通知 |
| `onComplete` | 回呼函數，用於非同步操作完成後通知 |
| `deviceId` | 裝置 ID，用於 Firebase 回音過濾 |
| `PRODUCTION` | 是否為生產模式（控制日誌輸出） |
| `UI_COLORS` | UI 顏色常數物件 |

---

## 5. CSS 設計令牌

### 5.1 背景色系

```css
--bg-base:       #0a0a0f;  /* graphite black · 主背景 */
--bg-surface:     #0d1117;  /* midnight blue black · 表面 */
--bg-elevated:   #161b22;  /* deep slate · 卡片/彈出層 */
--bg-inset:      #1c2129;  /* 內嵌面板 */
--bg-row-odd:    rgba(255,255,255,0.02);
--bg-row-even:   rgba(255,255,255,0.015);
```

### 5.2 邊框色系

```css
--border-subtle:  rgba(255,255,255,0.06);
--border-default: rgba(255,255,255,0.08);
--border-strong:  rgba(255,255,255,0.12);
--border-focus:   var(--tech-cyan);
```

### 5.3 文字色系

```css
--text-primary:   #e6edf3;  /* 主文字 · 高可讀 */
--text-secondary: #8b949e;  /* 次要文字 */
--text-muted:     #6e7681;  /* 弱化文字 */
--text-placeholder: #484f58;  /* placeholder */
```

### 5.4 科技冷色點綴

```css
--tech-cyan:     #00d4ff;  /* 科技青 · hover/active/焦點 */
--tech-cyan-dim: rgba(0,212,255,0.12);
--sky-blue:      #0095ff;  /* 天藍 · 圖表/高亮 */
--sky-blue-dim:  rgba(0,149,255,0.12);
--electric-violet:#7c3aed;  /* 電紫 · accent */
--electric-violet-dim: rgba(124,58,237,0.12);
```

### 5.5 中式點綴（金/紅）

```css
--gold-subtle:   #8B6914;  /* 黛金 · 極淡點綴 */
--gold-soft:     #c9a84c;  /* 柔金 · 次要裝飾 */
--gold-muted:    rgba(201,168,76,0.15);
--vermilion:     #c0392b;  /* 朱砂紅 · 危險/警告 */
--vermilion-soft:rgba(192,57,43,0.12);
```

### 5.6 狀態色

```css
--success:       #2dd4a0;  /* 綠 · 成功 */
--success-dim:   rgba(45,212,160,0.10);
--warning:       #f0a500;  /* 琥珀 · 警告 */
--warning-dim:   rgba(240,165,0,0.10);
--danger:        #f85149;  /* 紅 · 錯誤 */
--danger-dim:    rgba(248,81,73,0.10);
--info:          #58a6ff;  /* 藍 · 資訊 */
--info-dim:      rgba(88,166,255,0.10);
```

### 5.7 圓角尺度

```css
--radius-xs:  4px;
--radius-sm:  8px;
--radius-md:  12px;
--radius-lg:  16px;
--radius-xl:  20px;
--radius-pill: 999px;
```

### 5.8 間距尺度

```css
--sp-1: 4px;
--sp-2: 8px;
--sp-3: 12px;
--sp-4: 16px;
--sp-5: 20px;
--sp-6: 24px;
--sp-8: 32px;
```

---

## 6. 資料架構

### 6.1 交易物件 (Transaction)

```javascript
{
  _fbKey: "Lxyz123...",    // Firebase 唯一金鑰
  id: 1,                   // 數字 ID（向後相容）
  date: "2026-06-09",      // 日期
  dow: "週一",              // 星期幾
  type: "rolling",          // 類型："rolling" 或 "cash"
  agent: "代理A",           // 代理名稱
  client: "客人B",          // 客人名稱（洗碼用）
  venue: "XX賭場",          // 場地
  volume: 100000,          // 碼量
  rate: 1.24,             // 返佣率 (%)
  comm: 12400,            // 佣金
  bonus: 2000,            // 加碼
  drawn: 1000,            // 已領
  undrawn: 1000,          // 未領
  fund: 10400,            // 公基金 (comm - bonus)
  cash: 0,                // 現金寄放金額
  note: "備註",            // 備註
}
```

### 6.2 公基金紀錄 (Fund Withdrawal)

```javascript
{
  _fbKey: "Lxyz456...",
  id: 1,
  date: "2026-06-09",
  type: "withdraw",       // "withdraw" | "deposit" | "cash_deposit"
  amount: 5000,
  note: "備註",
}
```

### 6.3 代理錢包紀錄 (Agent Wallet)

```javascript
// agentWallets[agentName] = [ ...records ]
{
  _fbKey: "Lxyz789...",
  id: 1,
  date: "2026-06-09",
  type: "withdraw",       // "withdraw" | "deposit" | "cash_deposit"
  amount: 3000,
  note: "備註",
}
```

### 6.4 房務訂房 (Room Booking)

```javascript
// rmBookings = [ ...bookings ]
{
  id: 1,
  casino: "XX賭場",
  hotel: "YY酒店",
  roomType: "標準房",
  checkIn: "2026-06-09",
  checkOut: "2026-06-10",
  nights: 1,
  price: 500,
  total: 500,
  note: "備註",
  createAt: "2026-06-09T10:00:00.000Z",
}
```

---

## 7. Firebase 架構

### 7.1 資料路徑

```
macau_data/
├── txs/                    # 交易資料
│   ├── Lxyz123... { ... }  # 每筆交易以 _fbKey 為路徑
│   └── Lxyz456... { ... }
├── fundWithdrawals/        # 公基金紀錄
│   └── Lxyz789... { ... }
├── agentWallets/           # 代理錢包（已廢除，改用 localStorage）
├── agentList/              # 代理清單（已廢除，改用 localStorage）
├── rmBookings/            # 房務訂房
│   └── Labc123... { ... }
├── hcConfig/              # 酒店配置
│   └── casino_hotel { ... }
└── config/                # 全域配置
    └── workingMonth: "2026-06"
```

### 7.2 同步流程

1. **上傳 (syncUpload)**：
   - 讀取 localStorage 資料
   - 加密後寫入 Firebase
   - 使用 `_syncSet()` 重試封裝（最多 3 次，指數退避）

2. **下載 (syncDownload)**：
   - 監聽 Firebase `value` 事件
   - 解密後寫入 localStorage
   - 使用 `deviceId` 回音過濾（避免無限循環）

3. **初始化 (initSync)**：
   - 監聽 `macau_data/txs` 變更
   - 監聽 `macau_data/fundWithdrawals` 變更
   - 監聽 `macau_data/agentWallets` 變更
   - 監聽 `macau_data/agentList` 變更
   - 監聽 `macau_data/hcConfig` 變更

---

## 8. 鍵盤快捷鍵

| 快捷鍵 | 功能 |
|--------|------|
| `Ctrl + N` | 新增交易 |
| `Ctrl + S` | 儲存 |
| `Ctrl + 1` ~ `Ctrl + 5` | 切換頁面（總覽/全部交易/查詢/統計/房務系統）|
| `?` | 顯示快捷鍵說明 |
| `Escape` | 關閉彈窗 |
| `Enter` | 確認（密碼輸入、表單提交）|

---

## 9. 版本沿革

### v11.1.8 (2026-06-09) — 正式版
- ✅ 恢復刪除確認彈窗 (`confirm("確定刪除此筆交易？")`)
- ✅ 移除刪除後的「已刪除 N 筆交易」撤銷提示
- ✅ 移除整個多級撤銷系統
- ✅ 保留資料編輯後的「同步中 → 同步成功」提示

### v11.1.7 (2026-06-09)
- ❌ 誤刪除確認彈窗（已回復）
- ✅ 恢復資料編輯後同步提示

### v11.1.6 (2026-06-09)
- ✅ 修復 `#page-room` 桌面版永遠顯示
- ✅ 修復手機版表格無法水平捲動
- ✅ 包覆所有表格於 `.table-scroll` 容器

### v11.1.5 (2026-06-09)
- ✅ 修復電腦版「密碼錯誤或資料損毀」Toast
- ✅ 新增 `silent` 參數至 `decryptData()`/`decryptWallets()`

### v11.1.4 (2026-06-09)
- ✅ 修復 7 項錯誤（同步假成功、酒店配置未同步、rmBookings lastId、agentList 重新整理、CSV 格式、總覽篩選不一致）

### v11.1.3 (2026-06-09)
- ✅ 修復 `#hc-modal-bg` CSS 遺失

### v11.1.2 (2026-06-08)
- ✅ Firebase push() 金鑰架構
- ✅ AES 加密/解密
- ✅ 模組化重構

### v10.26 (基準版)
- 📌 黃金基準版本（永不修改）

---

## 10. 已知問題與待辦事項

### 10.1 待修正（v11.2）

- [ ] `js/pages/query.js`：30+ 處硬編碼顏色值
- [ ] `js/pages/overview-charts.js`：圖表顏色配置
- [ ] `js/pages/overview.js`：場地分佈顏色
- [ ] `js/pages/rank-chart.js`：排名顏色

**策略**：v11.2 中用 `UI_COLORS` 常數批次取代，或改為 className 方式。

### 10.2 模組化遺留教訓

v11 模組化拆分時遺失了兩批 CSS：
1. **v11.1.1 修復**：`.rm-*` 房務系統桌面端 CSS（~280 行）
2. **v11.1.3 修復**：`#hc-*` 酒店配置彈窗 CSS（~45 行）

> ⚠️ **後續注意**：任何 Modal 必須有 `display:none` 預設規則！

---

## 11. 部署與測試

### 11.1 部署流程

```bash
# 1. 修改模組來源檔案（css/*.css 或 js/**/*.js）
# 2. 執行建構
node build.js

# 3. 提交並推送
git add .
git commit -m "v11.1.8 — 描述"
git push

# 4. Railway 自動部署
# 部署網址：https://macau-sync-production-4a7b.up.railway.app
```

### 11.2 測試清單

- [ ] 登入功能（密碼：`macau888`）
- [ ] 新增交易（洗碼 + 現金寄放）
- [ ] 編輯交易
- [ ] 刪除交易（確認彈窗）
- [ ] 同步功能（同步中 → 同步成功）
- [ ] 公基金管理
- [ ] 代理錢包
- [ ] 代理管理
- [ ] 備份/還原
- [ ] CSV 匯入/匯出
- [ ] 房務系統
- [ ] 酒店配置
- [ ] 手機版側邊欄
- [ ] 手機版表格水平捲動
- [ ] 快捷鍵 (`Ctrl+N`, `Ctrl+S`, `?`)

---

## 12. 重要規則

1. **永遠修改模組來源檔案**（`css/` 或 `js/`），不要直接改 `dist/index.html`
2. **修改後必須執行 `node build.js` 重新建構**
3. **每次修改前先備份**：`copy index.html index_backup_YYYYMMDD.html`
4. **黃金基準檔案永不修改**
5. **任何 Modal 必須有 `display:none` 預設規則**
6. **ID 選擇器特異性陷阱**：`#page-room` 會覆寫 `.page`（必須用 `#page-room.active`）

---

*本文件由 WorkBuddy AI 自動生成，記錄 v11.1.8 正式版完整資訊。*
